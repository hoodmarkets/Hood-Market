name: Pull request
on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: pr-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  qa:
    name: QA
    uses: ./.github/workflows/qa.yml
    secrets: inherit
